# 적대적 검증 리포트 — 홍승아 기술 블로그 PRD·사용자 스토리

- 검증 대상: `.claude/_docs/tech-blog/PRD.md`, `.claude/_docs/tech-blog/user-stories.md`
- 검증 기준 리비전: `develop` @ `269db294` (문서가 적었던 `20131681`이 아님)
- 검증일: 2026-08-03
- 성격: 칭찬 금지·결함 색출. 결함 0인 축은 "왜 없다고 판단했는지" 근거를 명시.

> **후속 검증 메모**: 리포트 작성 시점에 "미검증"으로 남았던 2건은 이후 오케스트레이터가 실측했다 — **태그 41종**(`sort -u` 실측 41), **OG 이미지 1200×630**(`file public/og/*.png` 실측 일치). 둘 다 문서 주장과 일치하므로 D22·D23의 "미검증" 지적은 해소되었고, 검증 수단 부재(자동 회귀 신호 없음)만 남는다.

---

## 1. 재구성한 요구↔스토리 추적 매트릭스

문서의 요약표를 믿지 않고 각 스토리의 `covers:` 태그를 직접 수집해 역방향으로 재구성했다.

| 요구 | 커버 스토리 (재구성) | 상태 |
|---|---|---|
| R1 | S1 | 커버 |
| R2 | S2 | 커버 |
| R3 | S3 | 커버 |
| R4 | S4 | 커버 |
| R5 | S5 | 커버 |
| R6 | S6 | 커버 |
| R7 | S7 | 커버 |
| R8 | S8 | 커버 |
| R9 | S9 | 커버(부분충족 명시) |
| R10 | S10 | 커버 — 단, HEAD의 새 불변식 미반영(D4) |
| R11 | S10 | 커버 |
| R12 | S11 | 커버 |
| R13 | S12 | 커버 |
| R14 | S13 | 커버 |
| R15 | S14 | 커버 — 기준 리비전 판정 오류(D5) |
| R16 | S15 | 커버(부분충족 명시) |
| NFR1 | S16 | 커버 |
| NFR2 | S9 | 커버 — 부적절 배치(D11) |
| NFR3 | S4 | 커버 |
| NFR4 | S17 | 커버 — AC 부정확(D9) |
| NFR5 | S12 | 커버 |
| NFR6 | S6 | 커버 |
| NFR7 | S18 | 커버 — 범위 축소(D3) |
| NFR8 | S16 | 커버 |
| NFR9 | S19 | 커버 — 깊이 부족(D5), 값 규약 누락(D21) |
| NFR10 | S17 | 커버 — 수치 전부 오류(D2) |
| NFR11 | S20 | 커버 |

**요구 태그 기준 커버율: 27/27 = 100%.** 문서의 "미커버 요구 0건" 주장은 태그 수준에서는 참이다.

그러나 매트릭스의 **행 자체가 불완전하다.** PRD §3.1(In 범위)에 명시된 다음 항목들은 어떤 R/NFR로도 승격되지 않아 매트릭스에 행이 아예 없다 — 즉 "미커버 0건"은 행 집합이 좁아서 나온 수치다.

| 범위 항목(§3.1 In) | 대응 요구 | 대응 AC | 자동 검증 |
|---|---|---|---|
| 검색 콘솔·네이버 소유권 확인 메타 태그 | 없음 | 없음 | 없음 |
| 마크다운 콘텐츠 이미지 정적 동기화(`public/blog-assets`) | 없음 | 없음 | 없음 |
| canonical↔sitemap `<loc>` 일치(HEAD 신규) | 없음 | 없음 | `seo.test.ts` 있음 |
| Vitest 스펙 규모 | 없음(E2E만 NFR10) | 없음 | — |

**범위 기준 실질 커버율: PRD §3.1 In 13개 항목 중 4개 미커버.**

목표↔요구 방향도 재구성했다. PRD에는 목표↔요구 매핑 표가 **존재하지 않아** 다음은 요구 문장에서 역추론한 것이다.

| 목표 | 내려온 요구 | 판정 |
|---|---|---|
| UG1 1~2회 조작 내 도달 | R1, R3, R4 | 연결됨 |
| UG2 완독 환경 | R2, R7, R8 | 연결됨 |
| UG3 모국어 읽기 | R5, R6 | 연결됨 |
| UG4 스크린리더·키보드 | R13, NFR5 | 축소(D6) |
| 비즈니스1 브랜딩 | R12, R10 | 연결됨 |
| 비즈니스2 지식공유 | R10, R11 | 연결됨 |
| 비즈니스3 학습 아카이브 | R1, R3, R4 | 연결됨 |
| — (고아) | R9, R14, R15, R16, NFR1·2·3·8·9·11 | 어떤 목표에도 연결 서술 없음(D7) |

---

## 2. 결함 목록

### 심각도: 높음

**D1 — 기준 리비전이 스테일. 두 문서 전체의 "관측된 사실" 주장이 현재 코드와 대조되지 않은 상태다.**

- 위치: `PRD.md` 헤더, `user-stories.md` 헤더 (`기준 리비전: develop @ 20131681`)
- 근거: HEAD는 `269db294 fix: 사이트맵과 어긋나던 canonical과 특수문자 슬러그 soft 404 수정`. 이 커밋은 `e2e/playwright/seo.test.ts`(신규 94줄), `src/lib/routes.test.ts`(신규 66줄), `src/app/[lang]/layout.tsx`, `src/app/[lang]/posts/[slug]/page.tsx`, `src/app/sitemap.ts`, `src/lib/jsonld.ts`, `src/lib/metadata.ts`, `src/lib/routes.ts` 8개 파일을 바꿨다. user-stories는 스스로 "회귀 기준"을 표방하는데, 기준선이 HEAD보다 1커밋 뒤라면 회귀 판정에 쓸 수 없다.
- 수정 제안(additive): 두 문서 헤더의 리비전을 `269db294`로 갱신하고 `리비전 이력` 절을 신설한다. 기존 본문은 D2·D4·D5 수정만 적용하고 나머지는 보존한다.

**D2 — NFR10과 S17의 E2E 수치 3개가 전부 실측과 다르다.**

- 위치: `PRD.md` NFR10 / §3.1 / SM4, `user-stories.md` S17 AC / 요약표

| 항목 | 문서 주장 | 실측(HEAD) |
|---|---|---|
| 스펙 파일 수 | 9개 | **10개** (`seo.test.ts` 추가) |
| `test()` 선언 | 24건 | **27건** (a11y 1 / analytics 3 / category 3 / i18n 3 / navigation 4 / post 3 / search 2 / seo 3 / tag 3 / theme 2) |
| 실행 기준 | 28건 | **31건** (27 − 1 + a11y 5페이지 루프) |

- S17의 마지막 AC `Then 28건이 전량 통과한다`는 지금 실행하면 **반드시 불합격 판정이 나온다** — 회귀 기준이 스스로 거짓 알람을 내는 상태다. (`pnpm e2e` 실제 실행은 하지 않음 → 통과 여부 자체는 미검증. 건수만 정적 실측.)
- 수정 제안(additive): 값만 교체하고 AC 문장에 판정 명령을 병기해 재발을 막는다.

**D3 — 검색엔진 소유권 확인 메타 태그가 In 범위·가정에는 있는데 요구·AC·자동 검증 어디에도 없다. 회귀 전례가 있는 자산이다.**

- 근거: `src/lib/site.ts`에 `verification.google`·`verification.naver`가 있고 `src/app/layout.tsx`에서 출력된다. 그러나 `grep -rn "verification|naver" e2e/` 결과 **0건**. NFR7의 근거는 `analytics.test.ts`뿐이고 S18의 AC 5건은 전부 gtag 한정이다. 저장소에 `4d1f4573 fix: 마이그레이션 때 유실된 검색엔진 소유권 확인 메타 태그 복구` 커밋이 있어 **이미 한 번 유실된 전력**이 있다.
- 수정 제안(additive): S18에 AC 1건 추가, NFR7 요구 문장에 괄호로 대상 명시.

**D4 — HEAD가 추가한 canonical↔sitemap 불변식과 그 회귀 게이트(`seo.test.ts`)가 어떤 요구·스토리에도 없다.**

- 근거: `269db294`가 canonical/hreflang/og:url을 상대 경로 → 절대 URL로 바꾸고(`src/lib/routes.ts:absoluteUrl`, `src/lib/metadata.ts`), `seo.test.ts`에 3개 테스트를 추가했다 — 사이트맵 URL 후행 슬래시, canonical↔`<loc>` 정확 일치, 특수문자 슬러그 렌더. S10의 현재 AC는 "존재한다" 수준이라 값이 상대 경로로 되돌아가도 전부 합격한다.
- 수정 제안(additive): S10에 AC 2건 추가. 기존 AC는 상위 조건이므로 병존.

**D5 — 기준 리비전에서 71편 중 5편(7%)의 상세가 soft 404였는데, 두 문서는 그 리비전에서 R1·R2·R15·UG1을 "충족"으로 판정했다.**

- 근거: `269db294` 커밋 — "정적 익스포트에서 `2020-02-17-c-c++` 같은 슬러그는 페이지 컴포넌트에는 퍼센트 인코딩된 채, generateMetadata에는 디코딩된 채 들어와 조회에 실패했다". 해당 슬러그 5편: `2020-02-17-c-c++`, `2025-06-29-vite6.0`, `2025-09-07-vite7.0`, `2026-07-13-vite8.0`, `2026-07-14-typescript7.0`. S19의 AC는 **목록까지만** 확인하므로 상세 라우트가 404여도 합격한다.
- 이 결함은 HEAD에서 수정됐다. 남은 문제는 ① 판정 깊이가 부족했다는 사실이 문서에 없다 ② 같은 결함이 재발해도 현재 AC 집합이 다시 통과시킨다.
- 수정 제안(additive): S19 AC에 상세 200 응답 조건을 덧붙이고, S1에 사이트맵 전수 도달성 AC를 추가한다.

### 심각도: 중간

**D6 — UG4의 판정 기준이 R13+NFR5로 축소되어, 스토리가 스스로 기록한 키보드 접근 갭이 어떤 요구의 미충족으로도 집계되지 않는다.** UG4는 "키보드만으로 이용"인데 판정 기준은 스킵 링크 + axe뿐이다. S2는 heading 앵커 포커스 미도달, S7은 진행률바 `aria-hidden`을 "관측된 사실"로 적었는데 R2·R7은 여전히 "충족"이다. axe는 hover-only 상호작용을 잡지 못하므로 NFR5 통과가 UG4 충족의 증거가 되지 못한다. → UG4 판정 범위 명시 + 가정 A7 신설.

**D7 — 목표↔요구 매핑 표가 없어 고아 요구와 지표↔요구 연결이 문서에 남지 않는다.** 역추론 결과 R9·R14·R15·R16·NFR1·2·3·8·9·11 총 10개가 어떤 목표에도 연결 서술이 없다. 특히 R16은 SM2의 유일한 데이터 공급원인데 그 연결이 없어, R16 부분충족 → SM2 영구 산출 불가라는 인과가 드러나지 않는다. → §2.3 매핑 표 신설.

**D8 — INVEST 자가점검의 S(Small) 판정 규칙이 스토리마다 다르게 적용됐다.** S4·S7·S10은 "관심사가 묶임"으로 S=X인데, S3(AC 10건, 최소 4관심사)는 S=O다. 반대로 S12·S16·S17은 **covers 태그가 2개씩인데도** S=O다. 판정 규칙이 없어 재현 불가능한 자가점검이다. → 규칙 1줄 명문화 + 해당 스토리 S 값만 재표기(분할하지 않음).

**D9 — S17의 배포 게이트 AC가 관찰 대상(잡 스텝)과 판정 내용(명령 5개)을 혼동한다.** 실측 `deploy.yml`의 스텝은 `Lint & typecheck`(lint·typecheck 한 스텝 두 줄) / `Unit tests` / `Build` / `Install Playwright browser` / `E2E tests`다. 문자 그대로 대조하면 불일치가 난다. → 관찰 대상을 "잡 스텝 내 실행 명령"으로 교체, "5단계"에 `(명령 기준)` 병기.

**D10 — RK2의 영향 분석이 불완전하다.** `deploy.yml`은 `on: push: branches: [master, main]`이다. 즉 `pnpm deploy`가 `main`을 만드는 순간 **deploy 워크플로가 트리거되어**, 소스가 아니라 정적 산출물만 든 브랜치에서 lint/build를 돌리려다 실패한다. "무해하게 무시되는 브랜치"가 아니라 CI 실패를 유발하는 경로다. (원격 `main` 부재 자체는 실측 확인됨.) → RK2 영향 셀 보강 + M4 판정에 트리거 브랜치 확인 추가.

**D11 — NFR2(네트워크 없는 빌드)가 부분충족 스토리 S9 하나에만 매달려 있다.** NFR2는 빌드 전역 제약인데 유일한 커버 스토리가 "인기 글 Top 5"이고, S9는 스스로 I=X·부분충족으로 표기된 스토리다. → S16에 병기(S9에서 떼지 않음 — 기존 매트릭스 보존).

**D12 — 콘텐츠 이미지 정적 동기화가 In 범위에 암시되어 있으나 요구·AC·검증이 전무하다.** `scripts/sync-content-assets.mjs`가 `contents/blog` 하위 자산을 `public/blog-assets`로 복사하고 그 산출물은 gitignore된 파생물이다. 동기화가 깨지면 **커밋 diff에 아무 흔적 없이 전 글의 이미지가 사라진다.** S19는 `assets/`를 "목록에서 제외"로만 다뤄 정반대 관점(서빙 대상)이 빠졌다. → S19에 AC 1건 추가 + NFR9 문장 보강.

**D13 — 관찰 주체·합격선이 없어 제3자 판정이 갈리는 수용기준 6건.**

| AC | 위치 | 판정 불가 지점 |
|---|---|---|
| `빌드 중 외부 API 호출 없음` | S9 | "네트워크 차단 환경"의 수준 미정의, 관찰 수단 없음 |
| `비프로덕션 호스트… 히트 전송 안 됨` | S18 | "비프로덕션 호스트" 정의 없음, 관찰 수단 없음 |
| `서빙 가능한 정적 파일만 생성` | S16 | 판정 절차 없음 |
| `자리표시자가 실제 값으로 치환` | S11 | **실제 값이 무엇인지 없음.** 문서 내 71과 142가 공존해 판정 불가 |
| `다른 섹션이 화면에 들어온다` | S7 | IntersectionObserver 임계 미정의 |
| `토큰별로 색이 구분된 구문 강조` | S2 | 육안 판정, 합격선 없음 |

**D14 — S1의 "필터 결과 0건" 경계 AC는 현재 콘텐츠로 재현할 수 없다.** 실측 카테고리 분포는 `개발 69 / 문서 1 / tools 1`(합 71)로 0건이 되는 카테고리가 없다. 도달 조건이 없어 합·불 판정을 낼 수 없다. → 재현 가능한 전제(`?category=존재하지않는값`)로 좁히거나 "관측된 갭 목록"으로 이동.

**D15 — SM4 ④의 "충족" 판정이 skip 경로를 통과로 집계한다.** S18은 `429/5xx면 skip`을 명시한다. 따라서 `analytics.test.ts`의 "통과"는 ① 측정 ID 생존 확인 ② 외부 장애로 확인 못 함을 구분하지 않는다. SM4가 "유일하게 실측 가능한 축"인 만큼 신뢰도에 직결된다. → 판정 방법에 skip≠충족 단서 추가 + 가정 A8 신설.

**D16 — S20의 마지막 AC는 제품 동작이 아니라 프로세스 약속이라 수용기준 자리에 있으면 안 된다.** 배포된 제품을 관찰해 판정할 수 없고 판정 시점도 미래다. 이 항목이 남아 있는 한 S20은 영원히 완전 통과를 받을 수 없다. → **AC에서 제거하고 PRD 가정/인계 사항으로 이동**(정보 손실 없음). 24건 중 유일하게 구조적 이동이 필요한 항목.

**D17 — UG1의 판정 기준이 목표를 "경로가 존재한다"로 약화시켜 사실상 반증 불가하다.** 존재 명제는 경로 하나만 있으면 항상 참이므로 UG1은 절대 불합격이 나오지 않는다. D5의 soft 404 상황에서도 이 기준은 합격이었다. → 측정 가능한 하한(사이트맵 전수 200 도달)을 덧붙인다.

### 심각도: 낮음

**D18 — R13의 근거 행번호가 어긋났다.** `layout.tsx:88`을 가리키는데 실측 `href="#main-content"`는 85행이다(`269db294`가 이 파일을 변경). 행번호 고정은 무관한 커밋마다 스테일해진다. → 심볼·문자열 기준 인용으로 전환.

**D19 — "JSON-LD 7종"이 최상위 타입 한정임이 명시되지 않았다.** 같은 파일에 `Person`·`ItemList`·`ListItem`·`Question`·`Answer`가 중첩 타입으로 존재해 전수로 읽는 검증자와 판정이 갈린다. → 계수 기준 괄호 추가.

**D20 — Vitest 스펙 규모가 어떤 요구·AC로도 계량되지 않는다.** 실측 Vitest 스펙 6개(`popular / reading-time / markdown / routes / posts / i18n`). E2E는 파일 수까지 고정하면서 유닛 테스트는 사라져도 신호가 없는 비대칭이다.

**D21 — 콘텐츠 규약(NFR9)이 프론트매터 값 규약을 다루지 않아 카테고리 표기가 이미 흔들린다.** 실측 ko 카테고리 `개발 69 / 문서 1 / tools 1` — 한국어 콘텐츠에 영문 소문자 `tools` 1건이 섞여 있고 영문 쪽은 대문자다. 필터 칩으로 그대로 노출되므로 사용자에게 보이는 결함이다.

**D22 — §1.1의 콘텐츠 통계 일부가 어떤 AC로도 검증되지 않는다.** "69편 개발 카테고리"는 실측 확인, "태그 41종"은 리포트 시점 미검증(→ 후속 실측 41 일치 확인). 어느 쪽도 AC에 없어 회귀 시 신호가 없다.

**D23 — OG 이미지 크기 주장이 미검증이다.**(→ 후속 실측 `1200 x 630` 일치 확인.) 또 AC가 "가리킨다"까지만 요구해 파일이 삭제돼도 합격한다. → 200 응답 조건 추가.

**D24 — 마일스톤 ID가 결번이다.** §6은 M0/M1/M2/M4로 M3가 없다. RK1↔M2·RK2↔M4 참조가 본문 여러 곳에 걸려 있어 재배열하면 참조가 깨진다. → 번호를 당기지 말고 결번 주석만 남긴다.

---

## 3. 축별 판정 요약

| 축 | 판정 | 근거 |
|---|---|---|
| **완전성** | 보완 필요 | 요구 태그 기준 27/27은 사실이나, PRD §3.1 In 13개 중 4개가 요구로 승격되지 않아 매트릭스 행 자체가 없다. "미커버 0건"은 좁은 행 집합의 산물(D3·D4·D12·D20). 목표↔요구 매핑 부재로 고아 요구 10개가 드러나지 않는다(D7). |
| **INVEST** | 보완 필요 | S 판정 규칙 미문서화로 동일 상황에 다른 값(D8). T 자가점검도 낙관적 — S1의 0건 경계 AC는 재현 조건 부재(D14), S18의 "비프로덕션 호스트"는 관찰 수단 부재(D13)인데 둘 다 T=O. 자인된 X(S6 T·S9 I·S20 E/T)는 정확하나 S20의 T=X는 표기가 아니라 AC 이동으로 해소해야 한다(D16). |
| **관찰성** | 보완 필요 | 대부분의 AC는 셀렉터·문자열·속성명까지 인용해 판정 가능하다. 다만 6건이 합격선·관찰 수단 부재로 갈린다(D13). 특히 S11의 "실제 값으로 치환"은 문서 안에 71과 142가 공존해 문서만으로 결정되지 않는다. 목표 층위에서는 UG1이 반증 불가 명제다(D17). |
| **일관성** | **보완 필요 — 가장 심각** | E2E 3개 수치 전부 불일치(D2). 기준 리비전이 HEAD보다 1커밋 뒤인 채 "관측된 사실"을 단언(D1). 기준 리비전에서 5편이 soft 404였는데 R1·R2·UG1 충족 판정(D5). RK2에 트리거 브랜치 사실 누락(D10). UG4↔R2·R7 모순(D6). |
| **모호·판정불가** | 경미 | 애매어는 대체로 제거되어 있다. 남은 것은 "네트워크 차단 환경", "비프로덕션 호스트", "데스크톱 폭(lg 이상)", "토큰별로 색이 구분", "5단계" — 전부 D13·D9에 흡수. 결함이 적다고 본 근거는 대다수 AC가 실제 DOM 속성(`aria-pressed`, `aria-current`, `data-theme`, `id="main-content"`)·리터럴 문자열·실행 명령을 인용해 해석 여지를 좁혔기 때문이다. |

### 대조 결과 결함 없음으로 확인된 항목

원문 71 / 전체 142 / `.en.md` 71, AI 크롤러 22종(이름까지 전부 일치), JSON-LD 최상위 7종, SM3 연도별 편수(2020:7 / 2021:4 / 2022:22 / 2023:13 / 2024:11 / 2025:4 / 2026:10, 합 71), `data/popular.json`이 `{updatedAt: null, rangeDays: 90, items: []}`, 원격 `main` 브랜치 부재, 검색 스코어링 가중치 100/50/30/20/8·최대 8건, 목록 정렬(게시일 내림차순 + 제목 오름차순), `next.config.ts`의 `output`/`trailingSlash`/`images.unoptimized`·basePath 부재, `pnpm@10.33.0`·`node >=20.9.0`, `refresh-popular` cron `0 18 * * *` + `workflow_dispatch`, 발행 대상 `gh-pages`, `not-found.tsx`의 기본 로케일 고정·공통 레이아웃 밖, `개발` 카테고리 69편.

---

## 4. additive-first 보완 권고 (우선순위 순)

**차단급 — 처리 전에는 두 문서를 회귀 기준으로 쓸 수 없다.**

1. **D1** 리비전 갱신 + `리비전 이력` 절 신설. 비파괴.
2. **D2** E2E 수치 3개 교체 + 판정 명령 병기(`ls`/`grep`)로 재발 방지. 값 교체만이므로 비파괴.
3. **D3** 소유권 확인 메타 태그 AC 1건 추가(S18). 회귀 전례가 있는데 검증이 0인 유일한 자산.
4. **D5** 상세 라우트 도달성 AC 추가(S19 보강 + S1 신규). `seo.test.ts`가 이미 부분 구현 → 문서를 코드에 맞추는 방향.
5. **D4** canonical↔sitemap AC 2건 추가(S10). 기존 AC와 병존 가능.

**구조 보완 — 신규 절 추가, 기존 표는 무변경.**

6. **D7** `§2.3 목표↔요구 매핑` 표 신설. 고아 요구 10개를 "제약·운영 기반"으로 명시하고 `R16 → SM2` 인과를 넣는다.
7. **D8** INVEST S 판정 규칙 명문화 + S3·S12·S16·S17의 S 값만 재표기. 스토리 분할은 하지 않는다.
8. **D6** UG4 판정 범위 명시 + 가정 A7 신설. 요구 상태는 뒤집지 않는다.

**정확성 보완 — 문장 단위 교체.**

9. **D13** 비관찰 AC 6건에 합격선·관찰 수단 추가. S11의 치환 값 확정(71/2020)은 문서 내 수치 혼란도 동시 해소.
10. **D9** S17 AC의 관찰 대상 교체 + `Install Playwright browser` 스텝 기재.
11. **D10** RK2 영향 셀에 트리거 브랜치 사실 추가, M4 판정 보강.
12. **D15** SM4 ④에 skip≠충족 단서 + 가정 A8 신설.
13. **D12** 이미지 동기화 AC 1건 추가(S19) + NFR9 보강.
14. **D11** NFR2를 S16에 병기(S9에서 떼지 않음).
15. **D14** S1의 0건 AC를 재현 가능한 전제로 좁히거나 갭 목록으로 이동.
16. **D16** S20 마지막 AC를 PRD 가정/인계로 이동.

**저위험 정리 — 묶어서 처리.**

17. **D18** 행번호 인용 → 심볼·문자열 인용 전환. 커밋마다 스테일해지는 구조적 취약점.
18. **D19** 계수 기준 괄호 / **D20** Vitest 6스펙 병기 / **D21** category 값 규약 / **D22** 통계 각주 / **D23** OG 200 응답 AC / **D24** M3 결번 주석.

**구조적 뒤집기가 필요한 항목은 D16 1건뿐이다.** 대안이 명확하다 — PRD 가정 또는 인계 사항으로 그대로 옮기면 정보 손실이 없고 S20의 T=X가 자연 해소된다. 나머지 23건은 전부 추가·보강으로 처리 가능하다.

---

## 5. 판정

- **미커버 요구(R/NFR 태그 기준)**: 없음 (27/27)
- **미커버 범위 항목(PRD §3.1 In 기준)**: 4건 — 검색엔진 소유권 확인 메타 태그, 콘텐츠 이미지 동기화, canonical↔sitemap 불변식, Vitest 스펙 규모
- **차단 결함(높음)**: D1, D2, D3, D4, D5 (5건)
- **종합: 보완 후 재검증.**

**미검증으로 남긴 항목**(숫자를 발명하지 않기 위해 명시): `pnpm e2e` 실제 통과 여부(건수만 정적 실측), `/sitemap.xml`의 실제 URL 구성과 `x-default` 값, GitHub Pages 저장소 설정의 서빙 소스(RK2가 이미 미확인으로 기재), GA4 속성 생존(A1), 검색 콘솔·네이버 소유권 유효성(A2), `refresh-popular` 시크릿 유효성(S15 충족 조건 시나리오).

> 후속 실측으로 해소된 미검증 항목: 태그 41종, OG 이미지 1200×630.
